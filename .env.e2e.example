# e2e 真机 profile 的 token 模板。复制为 .env.e2e（gitignored）后填值。
# ⚠ 这些 token 等同登录态——切勿提交、切勿外泄。.env.e2e 已在 .gitignore 内。

# ── online-prod profile：生产站 https://novita.ai 的用户 JWT ──
# 取法：浏览器登录 novita.ai → DevTools → Application → Cookies →
#       https://novita.ai → 复制名为 `token` 的 cookie 值，粘到下面。
E2E_TOKEN_PROD=

# ── local-live / online-staging profile：dev 环境用户 JWT（dev-api.novita.ai）──
E2E_TOKEN_DEV=

# 备注：
# - profile 选择与「生产确认」走命令行，不放这里（每次跑 prod 都显式确认）：
#     E2E_PROFILE=online-prod E2E_CONFIRM_PROD=1 npm run test:e2e:sweep
# - online-staging 默认域名是占位符，按真实 dev 域名覆盖：E2E_STAGING_URL=https://<dev域名>
