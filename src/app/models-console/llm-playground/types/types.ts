export enum ModelCapabilities {
  Text = "text",
  Image = "image",
  Audio = "audio",
  Video = "video",
}

export enum ModelFeatures {
  Reasoning = "reasoning",
}

export enum ChatMode {
  Chat = "chat",
  Completion = "completion",
  Response = "response",
}
