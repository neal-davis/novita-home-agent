import Section from "./components/section";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Novita AI GPU Instance storage",
};

export default async function Page() {
  return <Section />;
}
