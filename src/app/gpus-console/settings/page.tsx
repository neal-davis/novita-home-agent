import Section from "./components/section";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Novita AI GPU Instance settings",
};

export default async function Page() {
  return <Section />;
}
