import { Metadata } from "next";
import Section from "./components/section";

export const metadata: Metadata = {
  title: "Novita AI GPU Instance billing",
};

export default function Page() {
  return <Section />;
}
