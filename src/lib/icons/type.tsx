import { CSSProperties } from "react"

export type IconProps = {
  color?: string,
  frontColor?: string,
  backColor?: string,
  hoverColor?: string,
  width?: number,
  height?: number,
  opaque?: boolean,
  style?: CSSProperties
}