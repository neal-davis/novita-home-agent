# JieKou Toggle / ToggleGroup

**源码**：`src/components/ui/toggle.tsx`、`src/components/ui/toggle-group.tsx`。

- Toggle 基础圆角为 `rounded-md`。
- disabled 保持 `pointer-events-none opacity-50`。
- 图标型 Toggle 必须有可访问 label；不要用 emoji 作为 icon。
