# JieKou Badge

**源码**：`src/components/ui/badge.tsx`。

- 基础：`inline-flex items-center rounded-full border px-2.5 py-1 text-xs font-medium`。
- 支持 `default`、`secondary`、`destructive`、`outline`、`green` 等 variant。
- 状态语义优先使用已有 variant；不要只靠颜色表达错误/成功状态，必要时配文案或 icon。
