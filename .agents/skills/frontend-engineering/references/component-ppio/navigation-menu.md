# NavigationMenu

**源码**：`navigation-menu.tsx`。

| 节点                        | 规范                                                                                                         |
| --------------------------- | ------------------------------------------------------------------------------------------------------------ |
| **NavigationMenuViewport**  | **`rounded-md border bg-popover shadow-lg`**；全屏变体无 `border`，仅 `rounded-md bg-popover shadow-lg`。    |
| **NavigationMenuTrigger**   | **`rounded-md bg-background`**，`hover:bg-accent`、`data-[state=open]:bg-accent/50`，`disabled:opacity-50`。 |
| **NavigationMenuIndicator** | **`rounded-tl-sm bg-border`**（小菱形）。                                                                    |
