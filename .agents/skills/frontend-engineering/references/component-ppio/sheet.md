# Sheet

**源码**：`sheet.tsx`。

| 维度         | 规范                                                                                                                      |
| ------------ | ------------------------------------------------------------------------------------------------------------------------- |
| **内容区**   | `bg-background`；`sheetVariants.side`：顶/底 `border-t`/`border-b`，左/右 `border-l`/`border-r`（靠边全高，无整块圆角）。 |
| **遮罩**     | `bg-neutral-500/50`。                                                                                                     |
| **关闭按钮** | 与 Dialog 共用形态；**`data-[state=open]:bg-secondary`**。                                                                |
