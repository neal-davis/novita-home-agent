# ButtonGroup

**源码**：`button-group.tsx`。

| 子组件                   | 圆角 · 边框 · 背景 · 阴影                                                                |
| ------------------------ | ---------------------------------------------------------------------------------------- |
| **ButtonGroup**          | 子项 `rounded-l-none` / `rounded-r-none` 与去重相邻边框（`border-l-0` / `border-t-0`）。 |
| **ButtonGroupText**      | `rounded-md border bg-muted`、`shadow-xs`、`px-4`。                                      |
| **ButtonGroupSeparator** | `bg-input`（竖条；覆盖默认 Separator 的 `bg-border`）。                                  |
