# TimePicker

**源码**：`time-picker.tsx`、`time-picker.module.scss`。

| 维度                  | 规范                                                                                                                |
| --------------------- | ------------------------------------------------------------------------------------------------------------------- |
| **触发器**            | 与 DatePicker 同模式：`Button outline` + `.time_button` / `_active` / `_warning`（`--brand-0`、`--red-1`）。        |
| **下拉 `.time_list`** | `border: 1px solid var(--border-3)`、`border-radius: 4px`、`background: var(--white)`（未用 `bg-popover` 工具类）。 |
